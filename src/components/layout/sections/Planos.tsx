interface PlanosProps {
    id:string
}

const Planos = ({id}:PlanosProps) => {
    return (
        <section id={id} className="w-full h-screen bg-green-400">
            Enter
        </section>
    );
}

export default Planos;